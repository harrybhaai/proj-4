# proj-4
Quick memory anchors:

Setup → npm init -y && npm install express mongoose cors
Middleware trio → cors(), express.json(), express.static()
Every exam = Entity + Fields + CRUD → build the Model first, routes second, fetch last
